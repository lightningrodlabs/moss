import { test } from '../fixtures/moss';

test.skip('change UI language via settings', async ({ moss: _moss }) => {
  // why: exercises _new_design/moss-settings/ + @lit/localize runtime path.
  // Implement once smoke #2 is green so we have an active group to land in.
});
