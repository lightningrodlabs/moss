import { test } from '../fixtures/moss';

test.skip('switch between two groups via groups-sidebar', async ({ moss: _moss }) => {
  // why: depends on smoke #2 (create) and #3 (join) helpers being green.
  // Asserts each group's pane shows that group's tools.
});
